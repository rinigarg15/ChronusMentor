Highcharts.setOptions({
  exporting: {
    enabled: false,
    buttons: {
      contextButton: {
        menuItems: ['downloadPNG', 'downloadJPEG', 'downloadSVG', 'downloadCSV'],
      }
    },
    fallbackToExportServer: false,
    error: function(exportingOptions, error){
      ChronusValidator.ErrorManager.ShowResponseFlash("", jsHighChartOptions.downloadError);
    }
  }
});