<%# encoding: utf-8 %>
jQuery(function(){
  kendo.culture().calendar.months.names = fullCalendarParamHash.monthNames;
  kendo.culture().calendar.months.namesAbbr = fullCalendarParamHash.monthNamesShort;
  kendo.culture().calendar.days.names = fullCalendarParamHash.dayNames;
  kendo.culture().calendar.days.namesAbbr = fullCalendarParamHash.dayNamesShort;
});